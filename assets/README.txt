Drop your files here with EXACTLY these names:

  hero.jpg     — the opening full-screen photo (portrait, ~1200x2000 works well)
  couple.jpg   — the photo behind the names / returning-guest screen (portrait)
  music.mp3    — background audio (plays quietly, guest can mute)

All three are OPTIONAL. If a file is missing the page simply shows the
animated artwork instead and the music control hides itself — nothing breaks.
